#ERROR: [Bad step {'CALL': 'PROC', 'tests': ['R.status == 2']}] [unknowns call'attributes: ['CALL', 'tests']
root: http://test

PROC:
  - GET: /test

RUN:
  - CALL: PROC
    tests:
      - R.status == 2
