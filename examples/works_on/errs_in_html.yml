#! -do
root: http://test


TRY:
  - POST: /test?k_in_proc={{UNKNOWN}} # it's an error !
    body: "jjj"
    doc: test
    tests:
      - R.status == 201



RUN:
  - CALL: TRY
    # params: <<jjj>>

  - POST: /test         # it's an error !
    body: "jjj"
    doc: test
    tests:
      - R.status == 201
    params: <<totottt>>

  - POST: /test         # it's an error !
    body: "jjj"
    doc: test
    tests:
      - R.status == 201
    params:
     - i: <<toto>>

  - POST: /test?k={{UNKNOWN}} # it's an error !
    body: "jjj"
    doc: test
    tests:
      - R.status == 201

  - POST: /test
    body: "{{UNKNOWN}}"   # it's an error !
    doc: test
    tests:
      - R.status == 201

  - POST: /test
    body: "hll"
    doc: "{{UNKNOWN}}"    # it's not an error (doc are here for fun; not a problem for the request)
    tests:
      - R.status == 201


