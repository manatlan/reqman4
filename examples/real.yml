RUN:
  - GET: https://tools-httpstatus.pickup-services.com/500
    doc: this is the documentation of the test ... result is <b><i><<$status>></i></b>
    tests:
     - $status == 500

  - GET: https://tools-httpstatus.pickup-services.com/200
    doc: this is the documentation of the test
    tests:
     - $status == 200

  - POST: https://tools-httpstatus.pickup-services.com/201
    doc: this is the documentation of the test
    body:
      item:
        id: 42
        label: toto
    tests:
     - $status == 201
     - $status == 202
