#! -ode
# press CTRL+F5 to run this scenario with current py/repo (under vscode)


RUN:
  - GET: http://test/test?json=<<list("abc")>>
    doc:
        << tool.tablize( [R.json], ["col1","col2","col3"] ) >>
    tests:
      - R.status == 200

