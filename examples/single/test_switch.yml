#! --env2
host: http://test

switch:
  env1:
    doc: for dev
    host: http://dev/

  env2:
    doc: for verification
    host: http://verification/

  env3:
    doc: for validation
    host: http://validation/


RUN:

  - GET: /test?json=[1,2,3]
    tests:
      - len($) == 3
